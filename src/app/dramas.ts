export interface Drama {
    id: number;
    name: string;
    description: string;
    image: string;
    videoId: string;
    tags: string[],
    cast: string[],
    release: string,
    comments: string[]
}
